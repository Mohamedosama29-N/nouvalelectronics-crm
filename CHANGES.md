# ملخص التعديلات

## 1) تقسيم الكود
اتقسّم `src/App.jsx` (كان ~19,400 سطر) لأكتر من 60 ملف منظم تحت:
- `src/firebase/` — إعداد Firebase
- `src/constants/` — الأدوار، الصلاحيات، حالات التذاكر...
- `src/utils/` — كل الدوال المساعدة (تشفير، تصدير، بحث، إلخ)
- `src/hooks/` — useDebounce, useDarkMode, useKeyboardShortcuts
- `src/components/<feature>/` — كل شاشة في ملفها الخاص (inventory, tickets, pos, customers...)

`src/App.jsx` بقى ملف "قشرة" (shell) بسيط بيحمل الشاشات ديناميكيًا (React.lazy).

اتشالت 3 ملفات كانت باقية من محاولة تقسيم سابقة ناقصة ومكسورة (بتستورد من مسارات ومكتبات مش موجودة أصلًا في المشروع): `src/MainLayout.js`, `src/contexts/AppContext.js`, `src/1111`.

## 2) إصلاحات أمنية
- اتشالت القيم الافتراضية (fallback) المكتوبة صراحة في الكود لمفاتيح Firebase ومفتاح التشفير. التطبيق دلوقتي **يرفض يشتغل** لو `.env` ناقص بدل ما يشتغل بمفاتيح ضعيفة أو قديمة بصمت.
- اتشال `console.log` اللي كان بيطبع تفاصيل إعدادات Firebase حتى في نسخة الإنتاج.
- أضيف `.env.example` (من غير قيم حقيقية) عشان يبقى مرجع آمن للمشاركة/الرفع على git.

## 3) إصلاحات أداء (سبب تقل النظام مع تعدد المستخدمين)
- كل شاشات النظام (16 شاشة) بقت **code-split** عبر `React.lazy` — بتتحمّل أول ما المستخدم يفتحها بس، مش كلها مع أول تحميل للتطبيق. النتيجة: أول تحميل بقى أصغر بكتير (شاشات زي xlsx/pdf export/charts اللي كل واحدة فيها مئات الكيلوبايتات بقت في ملفات منفصلة بدل ما تتحمل كلها مقدمًا).
- listener الموظفين اللي كان مفتوح على كل الكوليكشن من غير حد أقصى (`onSnapshot(collection(db,'employees'))`) اتحط له `limit(500)`، فمع تعدد المستخدمين المتصلين مش كل تعديل موظف هيبعت كل السجلات تاني من غير داعي.

## 4) باگات حقيقية موجودة في الكود الأصلي (مش ناتجة عن التقسيم) - اتصلحت
اكتشفناها فعليًا لما شغّلنا `npm run build` + فحص ESLint (`no-undef`) على كل ملف كتأكيد إن الكود صحيح، مش مجرد تقسيم نظري:

1. **`EnhancedTicketManager`**: مفتاح `deliveryDate` كان مكرر مرتين في نفس الـ object الافتراضي للتذكرة (في مكانين)، فالقيمة التانية كانت بتلغي الأولى بصمت.
2. **`CustomerProfileView`**: كانت بتستخدم متغيرات `technicians`, `maintenanceCenters`, `callCenters` من غير ما تكون معرّفة في نطاقها (الأصل كانت متعرّفة جوه `EnhancedCustomerManager` بس مكنتش بتتبعت كـ props). يعني فتح بروفايل عميل معين وعرض بيانات الفني/المركز/الكول سنتر كان بيرمي `ReferenceError` ويوقف الصفحة. اتصلحت بتمرير القيم كـ props.
3. **`InvoiceRenderer`**: زرار "إرسال الفاتورة بالبريد الإلكتروني" كان بينادي دالة `generateInvoiceHTML` **مش معرّفة في أي مكان في الكود كله** — يعني الميزة دي كانت متعطلة تمامًا من الأول. اتضافت دالة بسيطة بتبني HTML للفاتورة عشان الميزة تشتغل فعليًا.

## 5) ملاحظات لسه محتاجة مراجعة (مش بلوكر، بس تحسينات)
فحص ESLint العام رجع ~90 تحذير/خطأ style (مش أخطاء تشغيل حقيقية) زي: متغيرات معلنة ومش مستخدمة، استدعاء `setState` مباشرة جوه `useEffect` في أكتر من مكان (ممكن يعمل re-renders إضافية)، ومكونات بتتعرّف جوه الـ render بدل برّه. دي مش بتكسر التطبيق لكن تستاهل مراجعة تدريجية.

## كيفية التشغيل
```bash
npm install
cp .env.example .env   # ثم املأ القيم الحقيقية (أو استخدم .env الموجود بالفعل)
npm run dev
```

---

# الجولة التانية من التحسينات

## 1) باگ حقيقي خطير اتلاقى واتصلح: circular import
تعليق (comment) بيذكر اسم `EnhancedTicketManager` جوه `TicketCard.jsx` خلّى
سكريبت التقسيم يفتكره استخدام حقيقي ويضيف `import` وهمي — وده عمل
**circular import** فعلي بين `TicketCard.jsx` و `EnhancedTicketManager.jsx`
(كل ملف بيستورد التاني). اتعمل فحص شامل على كل الملفات ولقينا 4 حالات تانية
مشابهة (استيرادات جت من ذكر اسم component جوه كومنت مش استخدام فعلي في
الكود) في: `CustomerProfileView.jsx`, `AuditLogManager.jsx`,
`InvoiceTemplateManager.jsx`. كلها اتصلحت.

## 2) أداء إضافي
- `limit(1000)` على listener مخزون الفرع في شاشة التحويلات (كان بيجيب
  المخزون كامل بدون حد أقصى - نفس مشكلة الموظفين اللي اتصلحت الجولة اللي فاتت).
- `React.memo` على `TicketCard` و `StatusSelectComp` — بيتعرضوا جوه قايمة
  تذاكر طويلة، فده بيمنع إعادة رسمهم كلهم لما أي حاجة تانية في الشاشة تتغير.

## 3) تنظيف الكود
- اتشال ~30 متغير/استيراد غير مستخدم (imports فاضلة، معاملات catch مش
  مستخدمة، إلخ) — صفر تأثير وظيفي، تحسين قابلية القراءة بس.
- إصلاح تحذير regex بسيط في `sanitizeDocId`.

## 4) أمان: مراجعة Firestore Security Rules
اكتُشفت 2 مشكلة حقيقية في `firestore.rules`:

1. **حرجة**: كوليكشن `employees` كان فيه `allow write: if true` — أي حد
   عنده Firebase config بتاع مشروعك (وهو موجود علنًا في أي نسخة منشورة من
   التطبيق) يقدر يكتب/يمسح أي مستند موظف مباشرة عن طريق Firebase SDK من
   غير ما يفتح التطبيق أصلًا. **اتصلحت** لتطلب `isAuthenticated()` على
   الأقل (نفس باقي الكوليكشنز).
2. **معمارية (محتاجة قرارك)**: التطبيق بيعمل `signInAnonymously()` لأي
   زائر قبل ما يشوف شاشة تسجيل الدخول بتاعة النظام نفسه، يعني
   `isAuthenticated()` بترجع `true` لأي حد فاتح الرابط، مش بس المستخدمين
   المسجلين فعليًا. نظام الصلاحيات (الأدوار) بيتفحص في React بس، مش في
   الـ rules. الإصلاح الجذري محتاج نقل تسجيل الدخول لـ Firebase
   Authentication حقيقي + فحص الدور في الـ rules نفسها — ده تغيير معماري
   أكبر محتاج اختبار حي على مشروعك، فمعملتوش هنا. تفاصيل كاملة في تعليق
   أعلى ملف `firestore.rules`.

## 5) اختبارات آلية (Vitest)
اتضاف `vitest` + 23 اختبار وحدة (unit tests) للدوال الأساسية النقية
(`utils/search.js`, `utils/format.js`) اللي مش محتاجة اتصال بـ Firebase.
تشغيلها:
```bash
npm test
```
أثناء كتابة الاختبارات، اكتشفنا سلوك حافة بسيط (مش باگ عملي): `formatDate`
بترجع النص `"Invalid Date"` بدل `"-"` لو اتبعتلها قيمة مش قابلة للتحويل
لتاريخ خالص (حالة نادرة جدًا عمليًا لأن بيانات Firestore دايمًا صحيحة).
موثّق في تعليق الاختبار نفسه.

## 6) CI (GitHub Actions)
اتضاف `.github/workflows/ci.yml` بيشغّل `lint` + `build` + `test`
تلقائيًا مع كل push/pull request، عشان أي خطأ زي الـ circular import اللي
اتصلح فوق يتلقط تلقائيًا بدل ما يتكشف بالصدفة.

## اللي لسه محتاج قرارك/وقتك (مش هحله من غير اختبار حي على مشروعك)
- **الانتقال لـ Firebase Authentication حقيقي** بدل تسجيل الدخول
  client-side + anonymous auth (موضّح في firestore.rules).
- إكمال نظام المزامنة أوفلاين (`syncManager`) ليغطي كل شاشات الكتابة.
- باقي تحذيرات ESLint من نوع `react-hooks/exhaustive-deps` (15 تحذير) —
  راجعتها ولقيتها كلها أنماط "sync state on prop/filter change" سليمة،
  مش أخطاء حقيقية، فمعدلتهاش تفاديًا لتغيير سلوك من غير اختبار حي.

---

# الجولة التالتة: تحديثات سريعة + بداية الفاتورة الضريبية (ETA)

## 1) تحديثات سريعة
- تحديث آمن لكل الحزم القديمة ضمن نفس النسخة الرئيسية (`npm update`) —
  اتحقق منه بـ build كامل.
- Dark Mode: كان مطبق على 27/30 شاشة، أُكمل على الـ 3 الباقيين
  (`LoadingSkeleton`, `LowStockView`) بنفس نمط الألوان.
- تصدير CSV اتضاف لـ 3 شاشات كانت ناقصاه فعليًا: `ReturnsManager`,
  `WarrantyAlertsView`, `EnhancedTransferManager`.

## 2) باگ حقيقي اتلاقى واتصلح: متغير خارج نطاقه
أثناء إضافة معرّف الفاتورة لتغذية تكامل ETA، لقينا `transRef` في
`CustomerProfileView.jsx` متعرّف جوه دالة `runTransaction` بس مستخدم
برّاها (`onCheckoutSuccess`) — ده كان هيرمي `ReferenceError` فعليًا لو
اتنفّذ. اتصلح بترحيل التعريف للنطاق الخارجي.

## 3) الفاتورة الضريبية (ETA) — الأساسات جاهزة، الجزء الحرج لسه محتاج تأكيد
راجع `ETA_EINVOICE.md` للتفاصيل الكاملة. باختصار:
- ✅ مصادقة OAuth2، الإرسال، البنية الأمنية (Cloud Function + Firestore
  rules تمنع أي كتابة مباشرة من المتصفح على حالة الفاتورة الضريبية).
- ⚠️ شكل مستند الفاتورة نفسه (`functions/eta/documentBuilder.js`) لسه
  **STUB تقريبي** — الشكل الدقيق والتوقيع الرقمي مش منشورين علنًا،
  محتاجين استجابة `GetDocumentTypeVersion` الفعلية من بيئة الـ Sandbox
  بتاعتكم قبل أي استخدام حقيقي.
- الميزة مخفية خلف `systemSettings.eta.enabled` (false افتراضيًا) لحد ما
  تتأكدوا إنها شغالة صح.

---

# الجولة الرابعة: إشعارات، استعادة كلمة سر، الملف المالي، نسخ احتياطي

## 1) إصلاح حقيقي لنظام الإشعارات
FCM tokens كانت بتتخزن بمعرّف تسجيل الدخول المجهول (anonymous UID)، مش
بمعرّف الموظف الفعلي - يعني مستحيل تستهدف إشعار لدور/موظف معين. اتصلح
(`utils/messaging.js`)، وأضيفت Cloud Functions حقيقية بتبعت إشعارات
فعلية عند: تذكرة جديدة (`onNewTicket`)، نفاذ مخزون
(`onLowStockCrossed`) - `functions/notifications/`.

## 2) استعادة كلمة السر - ميزة كاملة
كان الزرار موجود بس بيقول "كلم مدير النظام". دلوقتي فعلي بالكامل:
- طلب رابط استعادة بالإيميل (`utils/passwordReset.js`) - صالح 30 دقيقة.
- شاشة تحقق من الرابط وتحديد كلمة سر جديدة (`ResetPasswordScreen.jsx`).
- كوليكشن `password_resets` جديد + rules خاصة بيه.

## 3) الملف المالي (Finance Module) - ميزة جديدة كاملة
`components/finance/FinanceManager.jsx`:
- إيرادات (من `transactions`) - مرتجعات موافق عليها (من `returns`) -
  مصروفات = صافي الربح، لكل فترة زمنية (يوم/أسبوع/شهر/سنة).
- إدارة مصروفات بفئات (إيجار، رواتب، كهرباء، صيانة...) - إضافة وحذف.
- رسمين بيانيين (اتجاه الإيرادات اليومية، توزيع المصروفات حسب الفئة).
- تصدير CSV للمصروفات.
- صلاحيتان جديدتان `viewFinance`/`manageFinance` في نظام الأدوار.
- Firestore index جديد لدعم استعلام المرتجعات بالفترة الزمنية.

## 4) نسخ احتياطي تلقائي مجدول
`functions/backup/scheduledBackup.js` - نسخة احتياطية يومية كاملة
(3 صباحًا بتوقيت القاهرة) باستخدام Managed Export الرسمي بتاع Firestore
(مش قراءة كل مستند يدويًا - أقوى وأسرع وبيحافظ على أنواع البيانات
بالظبط). **محتاج إعداد يدوي قبل التفعيل**: إنشاء Cloud Storage bucket +
صلاحية IAM "Cloud Datastore Import Export Admin" - التفاصيل في تعليق
أعلى الملف نفسه.

## 5) تقرير يومي مجدول بالإيميل
`functions/reports/dailySummary.js` - ملخص يومي (المبيعات، التذاكر
الجديدة) بيتبعت الساعة 8 صباحًا على نفس webhook البريد المستخدم في باقي
التطبيق. المستلمون بيتحددوا من شاشة الإعدادات (حقل جديد "مستلمو التقرير
اليومي التلقائي").

## 6) خطة الانتقال لـ Firebase Authentication
`FIREBASE_AUTH_MIGRATION.md` - تصميم مكتوب (مش كود منفّذ عمدًا، زي ما
اتفقنا - تسجيل الدخول بيستخدمه كل الموظفين يوميًا ومينفعش نجرب فيه من
غير اختبار حي تدريجي). التصميم المقترح: Custom Tokens + Custom Claims
للأدوار، مع خطة تنفيذ تدريجية بمراحل واضحة.

## التحقق النهائي (الجولة الرابعة)
- build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅ | كل ملفات الـ
  Cloud Functions الجديدة اتفحصت بـ `node --check` ✅

---

# الجولة الخامسة: تحسين بصري + فحص أخطاء شامل

## تقييم صادق قبل أي تعديل
قبل ما أعمل أي تغيير بصري، راجعت هوية النظام الحالية (الألوان، الخط،
نصف الأقطار، الظلال) بدل افتراض إنها محتاجة تغيير جذري. الاكتشاف: النظام
أصلاً بيستخدم لوحة ألوان متماسكة ومميزة (تيل + رمادي دافئ + ألوان دلالية
واضحة للنجاح/الخطر/التحذير) وخط عربي احترافي (IBM Plex Sans Arabic) —
مش الشكل الافتراضي العام (indigo/purple) اللي معظم القوالب الجاهزة
بتستخدمه. فتجنبت عمل "إعادة تصميم" غير ضرورية كانت هتبوّظ هوية موجودة
بالفعل، ورّكزت على إصلاحات حقيقية بدل كده:

## 1) الرسوم البيانية كانت بتستخدم ألوان recharts الافتراضية
`AdvancedCharts.jsx` كان بيرسم بألوان بنفسجي/أخضر فاتح عشوائية (افتراضي
المكتبة) مالهاش علاقة بهوية النظام، وكمان **كانت ثابتة في الوضع الليلي**
(نص وخطوط الرسم بيضا/غامقة تابتة بغض النظر عن الوضع، فكانت بتبان غريبة
على خلفية غامقة). اتصلحت بالكامل:
- الألوان بقت من نفس لوحة النظام (تيل، زمردي، كهرماني، وردي...).
- الشبكة والمحاور والـ tooltip بقوا واعيين بالوضع الليلي فعليًا.

## 2) تنظيف console.log متروكة من التطوير
- `SettingsManager.jsx`: شيلت useEffect كامل كان مفيدوش غير طباعة نفس
  البيانات في الكونسول مع كل تغيير - كان بيسبب ضوضاء غير ضرورية.
- `App.jsx`: رسالة "Firebase connection successful" بقت تظهر في وضع
  التطوير بس (نفس معيار الإصلاح اللي اتعمل قبل كده في firebase/config.js).

## 3) فحص شامل لأخطاء "مفتاح مكرر" (نفس فئة الباگ اللي اتلاقى قبل كده)
عملت سكريبت فحص آلي على كل ملفات المشروع (60+ ملف) بحثًا عن أي حالة
تانية شبه باگ `deliveryDate` القديم. النتيجة: **مفيش أي حالة تانية** -
كل الحالات اللي ظهرت في الفحص الأولي (كانت كتير) اتراجعت يدويًا وطلعت
كلها إيجابيات كاذبة (objects مختلفة أصلاً وليها نفس أسماء الحقول، زي
مصفوفة أعمدة الجدول اللي كل عمود ليه `field/width/render`).

## التحقق النهائي (الجولة الخامسة)
- build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅


---

# الجولة الخامسة: تحديث بصري شامل

## 1) هوية بصرية جديدة
- **اللون الأساسي**: اتغيّر من indigo/purple (نمط قوالب SaaS الشائع جدًا)
  لـ **teal** غامق واحد متسق - أنسب لطبيعة شركة إلكترونيات، وأكثر تميزًا.
  التغيير اتطبق بشكل منظم عبر كل الـ 60+ ملف (912 استخدام).
- **التدرجات اللونية (gradients)**: كل أزرار وهيدرات الشاشات كانت بتستخدم
  تدرج indigo→purple كديكور متكرر في كل مكان (نمط "قالب جاهز" كلاسيكي).
  اتحولت لـ لون واحد ثابت (teal) في كل مكان، وبقى فيه لحظة تدرج واحدة
  مقصودة بس (خلفية شاشة تسجيل الدخول) بألوان مختلفة ومميزة.
- **الخط**: IBM Plex Sans Arabic بدل خط النظام الافتراضي - خط احترافي
  مصمم للعربي والإنجليزي مع بعض، مناسب لأنظمة الأعمال.
- **شريط التمرير**: كان فيه كلاس `custom-scrollbar` مستخدم في الكود من
  غير أي تعريف فعلي له (يعني مالوش تأثير خالص) - اتضاف تعريفه فعليًا.
- **وضوح التركيز بلوحة المفاتيح**: أضيف outline واضح لأي عنصر متفعّل
  بالـ Tab (معيار إتاحة أساسي كان ناقص).

## 2) باگان حقيقيان اتلاقوا واتصلحوا أثناء التغيير
النظام بيستخدم ألوان كـ "مفاتيح" لتمييز فئات مختلفة (أدوار المستخدمين،
حالات التذاكر) - فيه مفتاحين كانوا حرفيًا اسمهم "indigo"، ولو سبتهم
كده كانوا هيتحولوا لنفس لون العلامة التجارية الجديد (teal) ويبقوا مش
متمايزين عن أي زرار تاني في الشاشة:
1. دور "مسؤول المخزن الرئيسي" (badge لونه كان indigo) → اتغيّر لـ `sky`.
2. حالة تذكرة "تم الشحن ASC" (badge لونها كان indigo) → اتغيّرت لـ `sky`.

## 3) تنظيف
- اتشال `App.css` - ملف من قالب Vite الافتراضي (شعار React الدوّار) كان
  موجود من غير أي استخدام فعلي في التطبيق.
- عنوان الصفحة (`<title>`) كان "my-erp" - اتغيّر للاسم الفعلي.
- `lang="en"` في `<html>` اتغيّرت لـ `lang="ar" dir="rtl"` (كل محتوى
  التطبيق عربي RTL أصلًا).
- ألوان الـ PWA manifest ومتصفح الموبايل (`theme_color`) اتحدّثت لتطابق
  اللون الجديد.

## التحقق النهائي (الجولة الخامسة)
- build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅ | صفر بقايا indigo
  أو تدرجات purple في الكود ✅





---

# باگ حقيقي وخطير في التحويلات المخزنية (سباق بيانات)

## الباگ: race condition عند الموافقة على تحويل
في `EnhancedTransferManager.jsx` → `handleApprove`: القراءة جوه
`runTransaction` كانت بتتم بـ `getDocs()` العادية بدل `transaction.get()`.
Firestore مكنش بيتابع القراءة دي كجزء فعلي من الترانزاكشن، يعني ضمان
"لا تعارض" اللي هو أصلًا سبب استخدام transaction اتلغى عمليًا.

**التأثير:** لو اتعمل موافقة على تحويلين لنفس الصنف في نفس الوقت تقريبًا
(من فرعين مختلفين مثلًا)، الاتنين ممكن يقروا نفس الكمية القديمة ويخصموا
منها من غير ما النظام يلاحظ التعارض - فرق حقيقي في جرد المخزون بمرور
الوقت، وأكتر احتمالًا كل ما عدد المستخدمين المتزامنين يزيد.

**التأكيد إنه سهو مش تصميم مقصود:** نفس النمط بالظبط موجود صح في نقطة
البيع (`POSManager.jsx`) باستخدام `transaction.get()`. عملت فحص آلي شامل
على كل الملفات بحثًا عن نفس النمط في أي مكان تاني - مفيش حالة تانية.

**الإصلاح:** البحث عن الصنف (query) بقى بيتم قبل الترانزاكشن للحصول على
مرجع المستند، وبعدين القراءة الفعلية للكمية بتتم بـ `transaction.get()`
جوه الترانزاكشن - وده بيخلي Firestore يرفض العملية تلقائيًا (ويعيد
المحاولة) لو حصل تعارض حقيقي، بدل ما يسمح بكتابة بيانات متضاربة بصمت.

## التحقق النهائي
- build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅ | فحص شامل لنفس نمط
  الباگ في كل الملفات (مفيش حالة تانية) ✅

---

# إصلاح شاشة التحويلات: البحث كان بيفشل بصمت + إعادة بناء آلية الطلب

## الباگ الفوري: البحث بيعمل لا شيء
`handleSearchProduct` كانت بترجع (`return`) بصمت من غير أي رسالة لو
مصفوفة المخزون فاضية - يعني المستخدم بيدوس بحث ومفيش أي رد فعل خالص،
زي ما لاحظتوا بالظبط. اتصلح: دلوقتي بيرجع رسالة خطأ واضحة دايمًا.

## السبب الجذري: البحث كان بيدوّر في المكان الغلط
البحث كان دايمًا بيدوّر في **مخزون المستخدم نفسه**، مش في المخزن الرئيسي.
يعني موظف فرع مقدرش يطلب صنف مش موجود عنده أصلًا - بالظبط عكس المطلوب.

## إعادة البناء (حسب الطلب)
- **موظف فرع عادي**: البحث دلوقتي بيدوّر في **مخزون المخزن الرئيسي**،
  حتى لو الصنف مش موجود عنده هو خالص. عند الموافقة، بيتحول تلقائيًا:
  صنف جديد لو مش موجود، أو زيادة في الكمية لو موجود من قبل (المنطق ده
  كان شغال صح بالفعل في `handleApprove`، ملهوش علاقة بالباگ).
- **موظف المخزن الرئيسي/الأدمن**: يفضل يدوّر في مخزونه هو ويختار الفرع
  المرسل إليه (زي ما كان قبل كده - "دفع" مش "طلب").

⚠️ **ملحوظة مهمة للاختبار**: أي حساب دوره "admin" بيتعامل معاملة المخزن
الرئيسي تلقائيًا بغض النظر عن الفرع المعيّن له. عشان تجربوا آلية "الطلب
من المخزن الرئيسي" الجديدة، لازم تسجلوا دخول بحساب موظف (مش admin)
معيّن لفرع غير المخزن الرئيسي.

---

# فهارس Firestore ناقصة لشاشة المخزون (يوزر غير admin)

## الباگ
مستخدم غير admin (زي "mohamed" - مركز صيانة) لما يفتح شاشة "إدارة
المخزون"، الاستعلام بيضيف فلتر `warehouseId` (عشان يشوف مخزنه بس) فوق
فلاتر `isDeleted` و(البحث أو الترتيب بالاسم). التوليفة دي محتاجة فهرس
مركّب في Firestore مش موجود أصلًا - كان موجود بس نسخة من غيره فلتر
`warehouseId` (يعني شغالة للـ admin بس، مش لمستخدم مقيّد بمخزن معين).

## الإصلاح
اتضاف 4 فهارس مركّبة ناقصة لـ `firestore.indexes.json`:
- `(warehouseId, isDeleted, name)` - تصفح المخزون بدون بحث لمستخدم مقيّد بفرع.
- `(warehouseId, isDeleted, searchTokens array-contains)` - بحث لمستخدم مقيّد بفرع.
- `(isDeleted, searchTokens array-contains)` - بحث للـ admin (كل الفروع).
- `(isDeleted, serialNumber, quantity)` - كان مفقود من نسخة الفهرس القديمة.

## ⚠️ لازم تعملوا deploy تاني عشان الإصلاح ده يشتغل فعليًا
```
firebase deploy --only firestore:indexes
```
زي المرة اللي فاتت، هيسألكم على حذف فهارس موجودة فعلاً - **ردّوا No**
عشان محدش يتمسح.

---

# ملحوظة: البحث في التحويلات مش باگ جديد - مشكلة إعداد حساب الاختبار

لما جربتوا "mohamed" (مركز صيانة) في شاشة التحويلات، ظهرت رسالة "لم يتم
العثور على المنتج **في مخزنك**" - مش "في المخزن الرئيسي". ده معناه إن
الحساب ده **معيّن أصلًا على المخزن الرئيسي نفسه** (مش على فرع)، فالنظام
تعامل معاه كمستخدم مخزن رئيسي (بيدور في مخزونه هو، مش بيطلب من حد).

**عشان تجربوا آلية "الطلب من المخزن الرئيسي" الجديدة فعليًا:** روحوا
لشاشة "إدارة المستخدمين"، وتأكدوا إن الموظف اللي بتجربوا بيه معيّن على
**فرع حقيقي غير المخزن الرئيسي** (مش admin وده معيّن على warehouse تاني).

---

# باگ إضافي: مستندات مخازن قديمة بدون حقل "name"

## الاكتشاف
من فحص Firestore Console مباشرة، لقينا مستندات في كوليكشن `warehouses`
اسم الفرع فيها متخزن جوه `managers.name` بدل حقل `name` مباشر في
المستند نفسه (غالبًا نتيجة إدخال يدوي/استيراد قديم قبل ما شاشة "إدارة
المخازن" الحالية تتعمل). النتيجة: أي مكان في النظام بيعرض اسم الفرع ده
(الهيدر، طلبات التحويل، تعيين الموظفين، التصدير) كان بيظهر فاضي/undefined.

## الإصلاح
`App.jsx` دلوقتي بيدوّر على الاسم بالترتيب: `name` المباشر، وإلا
`managers.name`، وإلا بيعرض معرّف مختصر بدل ما يسيبه فاضي تمامًا.

⚠️ ده إصلاح **عرض بس** - مش بيغيّر بيانات Firestore نفسها. لو حابين
تصلحوا المستندات القديمة دي فعليًا (تضيفوا حقل name مباشر)، ينفع من
شاشة "إدارة المخازن" - افتحوا الفرع وأعيدوا حفظه.

---

# تأكيد: الإصلاح الأصلي شغال ✅ + باگ فهرس تاني في نفس الشاشة

## تأكيد من نتيجة الاختبار
من صورة الاختبار الفعلي: اسم الفرع بقى يظهر صح في الهيدر ("مركز صيانه-
سامح حسين")، ورسالة الخطأ بقت تقول **"في المخزن الرئيسي"** مش "بمخزنك" -
يعني إصلاح آلية الطلب من الجولة اللي فاتت شغال 100% كما هو مطلوب.

## باگ فهرس تاني (كان بيمنع تحميل قايمة الطلبات في نفس الشاشة)
من الـ Console: `FirebaseError: The query requires an index` على
كوليكشن `transfers`. السبب: الفهرس الموجود في `firestore.indexes.json`
كان معمول لاستعلام يفلتر على `fromWarehouseId` **و** `toWarehouseId`
مع بعض في نفس الوقت - لكن الكود الفعلي بيفلتر على **واحد منهم بس** في كل
مرة (حسب دور المستخدم)، فالفهرس القديم مكانش هيغطي أي من الحالتين
الحقيقيتين خالص من الأساس. اتصلح باستبداله بفهرسين منفصلين يطابقوا
الاستعلامين الفعليين بالظبط.

## ⚠️ لازم deploy تاني
```
firebase deploy --only firestore:indexes
```
ردّوا **No** على أي سؤال حذف زي المعتاد.

---

# السبب الجذري النهائي: كل المخزون بدون حقل warehouseId

## الاكتشاف (بعد فحص مباشر في Firestore Console)
تأكدنا إن **كل أصناف المخزون** عند العميل من غير حقل `warehouseId` خالص
(مش بس بعض الأصناف القديمة). أي استعلام بيفلتر بالمخزن
(`where('warehouseId','==', ...)`) - يعني شاشة المخزون لغير الأدمن،
التحويلات المخزنية، وأي فلترة بالفرع - **بيختفي منه كل صنف تمامًا**، حتى
لو الصنف موجود فعليًا وبكمية كافية. ده سبب كل حالات "المنتج غير موجود"
اللي واجهناها.

## الإصلاح: توسيع أداة الإصلاح الموجودة أصلًا
شاشة "إدارة المخزون" فيها زرار "تحديث فهارس البحث (شامل)" (للأدمن بس)
كان بيصلح مشكلة مشابهة لحقل `isDeleted` الناقص. وسّعنا نفس الأداة عشان
كمان تضيف `warehouseId: 'main'` لأي صنف ناقصه الحقل ده (بافتراض إن كل
المخزون الحالي فعليًا في المخزن الرئيسي، وهو صحيح في هذه الحالة).

## ⚠️ خطوة لازم تتعمل مرة واحدة بعد نشر التحديث ده
1. سجّل دخول بحساب **admin**
2. روح لـ "إدارة المخزون"
3. دوس زرار **"تحديث فهارس البحث (شامل)"**
4. استنى لحد ما تظهر رسالة النجاح

بعد كده كل الأصناف هتبقى مربوطة بالمخزن الرئيسي، والبحث في التحويلات
والمخزون هيشتغل صح لكل المستخدمين.

---

# باگ UX حقيقي: البحث كان بيختار أول نتيجة تلقائيًا بدون تأكيد

## المشكلة
لو سيريالين مختلفين هما "903" و"19035"، البحث عن "903" كان بيستخدم
`.find()` اللي بيرجع **أول نتيجة مطابقة بس** ويختارها تلقائيًا - يعني
ممكن يختار "19035" غلط بدل "903" من غير ما يديك فرصة تشوف باقي النتائج
المطابقة أو تتأكد إنك اخترت الصنف الصح.

## الإصلاح
دلوقتي البحث بيجيب **كل** النتائج المطابقة:
- نتيجة واحدة بس → بتتحدد تلقائيًا زي الأول (مفيش داعي تدوس تاني).
- أكتر من نتيجة → بتظهرلك قايمة بكل الأصناف المطابقة (الاسم + السيريال +
  الكمية المتاحة) وتختار الصنف الصح بنفسك.

---

# تحسين احترافي عام في الشكل (تطبيق على كل الشاشات مرة واحدة)

بدل تعديل كل شاشة من الـ 30+ شاشة يدويًا (مخاطرة عالية لفايدة محدودة)،
اتعمل تحسين على المستوى العام (`index.css`) بينطبق على كل الشاشات فورًا:

## 1) انتقال سلس بين الوضع الليلي/النهاري
كان التبديل بيحصل فجأة بدون أي انتقال. دلوقتي الألوان (الخلفيات،
الحدود، النصوص) بتتغيّر بسلاسة، مع احترام كامل لإعداد "تقليل الحركة"
في نظام تشغيل المستخدم (accessibility).

## 2) استجابة لمسية للأزرار
كل زرار في النظام (بدون استثناء) بقى بيدّي إحساس ضغطة خفيفة فعلية عند
الدوس عليه - تفصيلة بسيطة بس بتفرق كتير في "إحساس" الاحترافية.

## 3) انتقال سلس عند التنقل بين الصفحات
كل صفحة بتظهر بحركة دخول خفيفة (fade + slight rise) بدل ما تظهر فجأة -
`key={currentView}` بتخلي الحركة تتكرر مع كل تنقل، مش مرة واحدة بس.

## 4) دعم شريط التمرير المخصص في Firefox
شريط التمرير الرفيع المتناسق مع هوية النظام كان شغال في Chrome بس -
دلوقتي شغال في Firefox كمان.

## 5) أدوات جاهزة للاستخدام المستقبلي
أضفنا utility classes (`animate-fade-in`, `hover-lift`) جاهزة، تقدر
تتضاف لأي بطاقة/عنصر جديد بسهولة من غير ما نكرر نفس الكود.

## ملاحظة مهمة
راجعنا شاشات زي الداشبورد قبل ما نعمل أي حاجة، ولقيناها أصلًا متصممة
كويس (hover effects، transitions موجودة بالفعل) - فمعملناش أي تعديل
عليها تفاديًا لمخاطرة كسر حاجة شغالة لفايدة بصرية بسيطة.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅

---

# إعادة تصميم شاشة نقطة البيع (POS) بشكل احترافي

## المشكلة
الشاشة كانت "فورم طويل" - كل حاجة (بحث، سلة، بيانات عميل، خيارات فاتورة،
إجمالي، أزرار) متكدسة فوق بعض في عمود واحد رأسي. ده مش شكل نقطة بيع
احترافية (زي Square أو Shopify POS) - الكاشير بيحتاج يشوف الإجمالي
والدفع طول الوقت وهو بيضيف أصناف، مش يقعد يلف لتحت كل مرة.

## التصميم الجديد: عمودين
- **العمود الرئيسي (يمين الشاشة تقريبًا، أوسع)**: شريط البحث + السلة -
  المنطقة اللي الكاشير شغال فيها طول الوقت.
- **العمود الجانبي (تحت البحث في الموبايل، جنب في الشاشة الكبيرة، ثابت
  أثناء التمرير - sticky)**: بيانات العميل، خيارات الفاتورة، الإجمالي،
  زرار الدفع - كله ظاهر طول الوقت من غير ما تحتاج تنزل لتحت.

## تحسينات إضافية
- سلة فاضية بقى ليها رسالة ودودة بدل ما تختفي تمامًا (كانت الشاشة قبل
  كده مش بتوري أي حاجة عن السلة لو فاضية).
- كل صنف في السلة بقى ليه أيقونة مميزة (قطعة غيار مقابل خدمة).
- الإجمالي بقى فيه فواصل واضحة بين كل بند (كان كله ملزوز في نفس الفقرة).

## ملاحظة مهمة
التصميم ده **بصري بالكامل** - كل الدوال والمنطق (البحث، الحساب، الدفع،
التحقق من المخزون) فضلت زي ما هي بالظبط من غير أي تعديل. اتحقق من
البناء والاختبارات بعد التعديل، وكل حاجة شغالة.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅ | صفر استيرادات غير
مستخدمة ✅

---

# إكمال: قطع الغيار من التذكرة بتخصم من المخزون فعليًا عند الفاتورة

## المشكلة اللي كانت باقية
بعد ما ربطنا قطع الغيار في التذكرة بالمخزون الحقيقي (الجولة اللي فاتت)،
لقينا إن الوصلة كانت بتتقطع في نقطتين قبل ما توصل نقطة البيع:

1. `handleGenerateInvoice` (في التذاكر) كان بيسقط `id`/`serialNumber`
   وقت تجهيز بيانات الفاتورة - يعني حتى لو القطعة مرتبطة بصنف حقيقي،
   المعلومة دي كانت بتضيع قبل ما توصل نقطة البيع.
2. نقطة البيع كانت بتعامل **كل** قطع الغيار الجاية من تذكرة كـ"خدمات"
   عامة بشكل ثابت، بدون أي خصم فعلي من المخزون - بغض النظر عن كونها
   مرتبطة بصنف حقيقي أو لأ.

## الإصلاح
- `handleGenerateInvoice`: بقى بينقل `id`/`serialNumber` كاملين.
- نقطة البيع (`POSManager`): دلوقتي بتتحقق من كل قطعة معاها `id` حقيقي
  مقابل **الكمية والسعر الفعليين في المخزون دلوقتي** (مش الكمية القديمة
  المحفوظة وقت إضافتها للتذكرة، اللي ممكن تكون اتغيرت):
  - لو موجودة وبكمية كافية → بتتحط في السلة كصنف حقيقي، وهتتخصم من
    المخزون فعليًا عند إتمام البيع.
  - لو نفذت الكمية أو الصنف اتحذف → بتتحط كخدمة عادية (بدون خصم مخزون)
    مع تنبيه واضح للكاشير إنها كانت مفروض تتخصم من المخزون.
  - القطع المضافة يدويًا (بدون ربط بالمخزون) تفضل خدمات زي ما كانت.

## التحقق النهائي (كل التعديلات في التذاكر ونقطة البيع)
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅ | صفر استيرادات دائرية ✅

---

# باگ حرج: مودال قطع الغيار كان موجود بالكود لكن مستحيل توصله

## المشكلة الحقيقية
بعد كل التحسينات اللي عملناها على قطع الغيار (بحث في المخزون، ربط
بالفاتورة، خصم من المخزون)، لقينا السبب الحقيقي وراء "مش ظاهر من جوا
التذكرة": **المودال بتاع قطع الغيار (بالبحث والجدول) كان موجود بالكامل
في الكود، لكن مفيش أي زرار في الشاشة كلها بيفتحه خالص**. يعني الميزة
كانت شغالة تقنيًا 100% لكن مستحيل توصلها من الواجهة - ده باگ "قابلية
وصول" (discoverability) حرج فاتني في التسليم اللي فات.

## الإصلاح
- ضيفنا زرار واضح "قطع الغيار" (مع عداد يوضح العدد الحالي) في شاشة
  تفاصيل التذكرة، جنب زراري "تعيين مسؤولين" و"تعديل التذكرة" مباشرة.
- اكتشفنا كمان إن إضافة قطعة غيار أو دفعة كانت بتتسجل صح في قاعدة
  البيانات، لكن **المودال نفسه مكنش بيتحدث فورًا** - كنت مضطر تقفله
  وتفتحه تاني عشان تشوف اللي أضفته. اتصلح، دلوقتي التحديث بيظهر لحظيًا.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅

---

# إكمال: اختيار قطع الغيار وقت إنشاء التذكرة نفسها (مش بعد كده بس)

## التوضيح من المستخدم
الميزة اللي اتضافت قبل كده (البحث عن قطع غيار من المخزون) كانت متاحة
بس **بعد** ما التذكرة تتحفظ (من شاشة تفاصيل التذكرة). المطلوب فعليًا:
إمكانية اختيار قطع الغيار **من نفس فورم إنشاء التذكرة**، قبل ما تتحفظ
أصلًا.

## الإصلاح
- نفس مكوّن البحث في المخزون (بالاسم/السيريال) بقى موجود جوه فورم إنشاء
  تذكرة جديدة كمان، مش بس شاشة التفاصيل بعد الحفظ.
- القطع المختارة بتتخزن محليًا لحد ما التذكرة تتحفظ فعليًا، وبعدين
  بتتحفظ معاها مباشرة كجزء من مستند التذكرة الأول (بدل ما كانت دايمًا
  مصفوفة فاضية `spareParts: []` بغض النظر عن أي حاجة).
- تكلفة التذكرة (`totalCost`) بقت بتتحسب تلقائيًا من قطع الغيار
  المختارة بدل ما تفضل صفر دايمًا.
- تقدر تعدّل الكمية أو تشيل أي قطعة قبل ما تحفظ التذكرة.

النتيجة: من لحظة إنشاء التذكرة، تقدر تحدد قطع الغيار المطلوبة، وبعدين
"إنشاء فاتورة" من نفس التذكرة هيحطها تلقائيًا في نقطة البيع مرتبطة
بالمخزون الحقيقي (زي ما اتصلح في التحديثات اللي فاتت).

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅

---

# باگ حقيقي: البحث عن قطعة غيار كان بيقفل فورم التذكرة بالكامل

## السبب
خانة البحث اللي اتضافت لقطع الغيار كانت `<form>` منفصل **جوه** فورم
إنشاء التذكرة الرئيسي (اللي هو نفسه `<form>` واحد كبير). HTML مبيسمحش
بـ`<form>` جوه `<form>` تاني - المتصفح بيتعامل مع الموقف ده بشكل غير
متوقع، وغالبًا بيبعت الضغطة لفورم التذكرة الرئيسي بدل فورم البحث، وده
اللي كان بيسبب الخروج المفاجئ من التذكرة اللي وصفته بالظبط.

## الإصلاح
اتشالت كل الـ `<form>` الفرعية الخاصة بالبحث عن قطع الغيار (في فورم
الإنشاء وفي مودال قطع الغيار)، واتستبدلت بزرار عادي (`type="button"`)
+ إمكانية الضغط على Enter في خانة البحث - نفس التجربة بالظبط، من غير
أي تعارض مع فورم التذكرة.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅ | فحصت الملف كامل
للتأكد إن مفيش أي `<form>` تانية متداخلة بنفس الطريقة

---

# ربط تلقائي لقطعة الغيار بكود العطل (من غير اختيار يدوي)

## الفكرة
كود العطل الفرعي أصلاً بيحمل "كود منتج" (`productCode`) مرتبط بيه (بييجي
من الاستيراد الأولي لشيت الأعطال). المستخدم اقترح إن الكود ده - لو
موجود - يتربط تلقائيًا بصنف حقيقي في المخزون بدل ما يدوّر عليه يدوي
في البحث.

## التنفيذ
- لما تختار كود عطل فرعي (سواء في إنشاء تذكرة جديدة أو تعديل تذكرة
  موجودة)، لو الكود ده معاه `productCode`، النظام بيدوّر في المخزون
  عن صنف بنفس السيريال تلقائيًا.
- لو لقاه، بيتضاف كقطعة غيار للتذكرة **تلقائيًا** (بسعره الحقيقي من
  المخزون)، مع تنبيه بسيط يوضح إنه اتضاف.
- نفس السلوك بيتكرر تلقائيًا مع كل كود عطل إضافي تضيفه لنفس التذكرة
  (بما إن كل اختيار كود بيعدي على نفس منطق الربط، الأساسي والإضافي
  بياخدوا نفس المعاملة من غير كود مكرر).
- مفيش تكرار: لو نفس الصنف اتربط قبل كده لنفس التذكرة، مبيتضافش تاني.

النتيجة: من لحظة اختيار كود العطل، قطعة الغيار المرتبطة بيه (لو
موجودة) بتكون جاهزة في التذكرة من غير أي خطوة يدوية إضافية - وتقدر
كمان تضيف قطع تانية يدويًا فوق كده لو محتاج.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅

---

# إصلاح: الربط التلقائي لقطعة الغيار كان بيفشل بصمت مع أكواد حقيقية

## السبب
من صورة فعلية بعتها المستخدم، "كود المنتج" اللي بييجي من كود العطل مش
دايمًا سيريال نضيف - أحيانًا بيكون وصف كامل زي "الفرشة كبيرة -4403A".
البحث كان بيدوّر بتطابق تام (`==`) مع السيريال بس، فكان بيفشل صامت مع
أي كود مش مطابق حرفيًا 100%، من غير أي رسالة توضح السبب.

## الإصلاح
- البحث دلوقتي بيجرب تطابق تام الأول (للأكواد اللي هي فعلاً سيريال
  صريح)، ولو فشل، بيرجع لبحث مرن بنفس أسلوب خانة البحث اليدوية (بالكلمات
  المقسّمة)، فبيلاقي الصنف حتى لو الكود وصف كامل مش سيريال حرفي.
- لو فعلاً مفيش أي صنف مطابق في المخزون خالص، بيظهر تنبيه واضح يقول
  كده بدل الصمت، عشان تعرف تضيف القطعة يدويًا بدل ما تفتكر إن فيه باگ.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅

---

# باگ حقيقي: استيراد شيت أكواد الأعطال مبيحدّثش السجلات الموجودة

## المشكلة
لما تعمل استيراد لشيت فيه "كود المنتج" لكود عطل **موجود بالفعل** من
استيراد سابق (قبل ما عمود كود المنتج يتضاف للشيت مثلاً)، الاستيراد كان
بيتجاهل الصف تمامًا ("⚠️ كود العطل موجود مسبقاً") - **حتى لو الشيت
الجديد فيه كود منتج مختلف أو مضاف لأول مرة**. يعني معملتش استيراد أول
بدون كود منتج، مستحيل تضيفه بعدين بإعادة الاستيراد - كان لازم تعديل كل
كود يدويًا واحد واحد.

## السبب التقني
الكود كان بيتتبع الأكواد الموجودة في `Set` (بس بيعرف "موجود ولا لأ")،
مش `Map` (بيحتفظ بالـ ID والبيانات الحالية) - يعني مفيش طريقة تقنية
أصلًا للرجوع للسجل وتحديثه، بس نعرف نتجاهله.

## الإصلاح
- بقى بيتتبع الأكواد الموجودة بكل بياناتها (المعرّف + الوصف + كود
  المنتج الحالي).
- لو الصف بتاع الاستيراد فيه كود منتج أو وصف **مختلف** عن المحفوظ،
  بيتم **تحديث السجل الموجود فعليًا** (`updateDoc`) بدل تجاهله.
- لو فعلاً مفيش أي اختلاف، بيتجاهل زي الأول (مفيش داعي كتابة غير ضرورية).
- ملخص الاستيراد بقى بيوضح عدد الأكواد اللي **اتحدثت** بجانب اللي
  اتعملت لأول مرة.

النتيجة: تقدر تعيد رفع نفس الشيت بعد ما تضيف/تعدّل أكواد المنتجات فيه،
وهيحدّث كل السجلات الموجودة تلقائيًا من غير ما تعدّلها واحد واحد يدويًا.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅

---

# إضافة خانة "كود المنتج" لشاشة إدارة أكواد الأعطال (الإعدادات)

## المشكلة
شاشة "إدارة المنتجات والموديلات" في الإعدادات كانت بتدير أكواد الأعطال
الفرعية بحقلين بس (الكود + الوصف) - رغم إن كود المنتج بقى جزء أساسي من
البيانات (سواء من الاستيراد أو الربط التلقائي بقطع الغيار). يعني مفيش
طريقة تشوف أو تعدّل كود المنتج المرتبط بكود عطل من غير ما تروح
لـ Firestore Console مباشرة.

## الإصلاح
- فورم إضافة كود عطل فرعي بقى بيه 3 حقول بدل 2: الكود، الوصف، **وكود
  المنتج** (اختياري).
- كل كود عطل في القايمة بقى معروض جنبه خانة كود المنتج، **قابلة للتعديل
  المباشر** (تكتب وتخرج من الخانة، بتتحفظ لوحدها من غير زرار حفظ منفصل).
- كده تقدر تشوف وتصحح أي كود منتج جه من الاستيراد مباشرة من نفس الشاشة،
  بدل ما تحتاج تدخل على قاعدة البيانات يدويًا.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅

---

# إضافة عداد "أكواد منتج مرتبطة" لشاشة استيراد الإكسل

## المطلوب
شاشة استيراد أكواد الأعطال كانت بتعرض 4 عدادات ملخّصة (منتجات، موديلات،
أكواد رئيسية، أكواد فرعية) بعد الاستيراد، بدون عداد خامس لعدد أكواد
المنتج (`product_code`) اللي اتربطت فعليًا - رغم إن العمود ده بقى جزء
أساسي من الشيت (5 أعمدة بدل 4).

## الإصلاح
اتضاف عداد خامس "أكواد منتج مرتبطة" بنفس شكل الأربعة عدادات الموجودة،
بيحسب عدد الصفوف اللي اتربط فيها كود منتج فعليًا (سواء وقت إنشاء كود
عطل جديد أو تحديث كود موجود). رسالة النجاح النهائية بعد الاستيراد كمان
بقت بتوضح الرقم ده.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅

---

# خانة خامسة مخصصة لكود المنتج في "إدارة المنتجات والموديلات"

## المطلوب
الشاشة كانت فيها 4 خانات (منتجات، موديلات، أكواد رئيسية، أكواد فرعية)
منسقين في شبكة 2×2. المطلوب خانة خامسة مخصصة لكود المنتج، بنفس أسلوب
باقي الخانات.

## التنفيذ
- خانة خامسة جديدة "كود المنتج" ظاهرة تحت الأربعة الموجودين (عرض كامل).
- تقدر تدوس على أي كود عطل فرعي من القايمة عشان "تختاره" (بقى قابل
  للاختيار زي الكود الرئيسي بالظبط، مش مجرد سطر للحذف بس).
- بعد الاختيار، تقدر تكتب/تعدّل كود المنتج بتاعه في الخانة الخامسة.
- زرار "معاينة" بيوريك **الصنف الفعلي في المخزون** اللي هيتربط (الاسم،
  السعر، الكمية المتاحة) قبل ما تحفظ - نفس أسلوب البحث المرن المستخدم
  في الربط التلقائي بالتذاكر، فمش محتاج كود مطابق حرفيًا 100%.
- زرار "حفظ" منفصل يحفظ الكود على كود العطل الفرعي المختار.
- كل كود عطل فرعي في القايمة بقى بيوري كود المنتج بتاعه (لو موجود) تحت
  اسمه مباشرة، عشان تشوف الحالة الحالية من غير ما تفتح الخانة الخامسة.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅

---

# استيراد أكتر من ملف مرة واحدة (Excel/CSV)

## الفكرة
منطق الاستيراد أصلاً مصمم يتعامل مع البيانات بشكل تراكمي (بيتأكد قبل ما
يضيف، وبيحدّث لو لقى تغيير) - فدمج أكتر من ملف قبل المعالجة آمن تمامًا
من غير أي تعارض بين الملفات.

## التنفيذ
- زرار اختيار الملف بقى بيقبل أكتر من ملف مرة واحدة (Excel أو CSV،
  ممكن تخلط بين النوعين في نفس العملية).
- كل الملفات بتتقرأ وتتجمع صفوفها في معاينة واحدة قبل الاستيراد.
- لو أكتر من ملف مختار، جدول المعاينة بيوري عمود إضافي يوضح كل صف جاي
  من أنهي ملف (يختفي تلقائيًا لو ملف واحد بس، عشان الشكل يفضل بسيط).
- ظهرت قايمة بأسماء الملفات المختارة فوق جدول المعاينة.

## باگ تاني اتلاقى وهو بيتصلح: حد أقصى 100 صف كان مخفي في المعاينة
أثناء التعديل، لقينا حد أقصى ثابت (100 صف) كان مطبّق على المعاينة من
غير أي تنبيه للمستخدم - يعني أي ملف فيه أكتر من 100 صف كان بيتقطع بصمت.
اتشال الحد ده تمامًا، خصوصًا إنه هيبقى عائق حقيقي مع دمج أكتر من ملف.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅ (بعد إصلاح مرجع متروك
لمتغير قديم اكتشفه فحص no-undef تحديدًا - الـ build العادي متعرفوش)

---

# إصلاح: الاستيراد كان بيبان واقف مع عدد كبير من الملفات/الصفوف

## المشكلة
مع 125 ملف و~2800 صف، الاستيراد كان بيبان واقف تمامًا من غير أي حركة -
رغم إنه كان شغال فعليًا في الخلفية.

## السبب الحقيقي (حاجتين)
1. **مرحلة قراءة الملفات**: كل الملفات كانت بتتقرأ مع بعض من غير أي
   مؤشر تقدم. قراءة ملفات Excel عملية بتستهلك معالجة فعلية (فك ضغط
   ZIP + تحليل XML)، فمع 125 ملف كان المتصفح فعلاً مشغول - بس الشاشة
   مكانتش بتوري أي تغيير خالص، فكانت تبان متجمدة.
2. **مرحلة الاستيراد الفعلي**: نافذة التحميل كانت شاشة كاملة ثابتة
   بتقول "هتاخد شوية ثواني" بس - مع 2800 صف (كل صف بيحتاج كتابة منفصلة
   لقاعدة البيانات)، العملية فعليًا بتاخد وقت أطول بكتير من "شوية
   ثواني"، ومفيش أي تحديث حقيقي يوري إن حاجة بتحصل.

## الإصلاح
- قراءة الملفات بقت **بالتتابع** (ملف بعد التاني) مع مؤشر تقدم حقيقي:
  "جاري قراءة الملفات... ملف 12 من 125" + شريط تقدم بصري.
- الاستيراد الفعلي بقى بردو بيوري تقدم حقيقي: "صف 340 من 2800" + شريط
  تقدم، بدل الرسالة الثابتة القديمة.
- جدول المعاينة بقى بيعرض أول 300 صف بس (لتفادي إبطاء المتصفح برسم
  آلاف الصفوف)، مع تنبيه واضح إن **كل** الصفوف هتتستورد فعليًا عند
  التأكيد - البيانات المستخدمة في الاستيراد نفسه فضلت كاملة زي ما هي.

## التحقق النهائي
build ✅ | 23/23 اختبار ✅ | صفر أخطاء no-undef ✅ | صفر تحذيرات lint
