# خطة الانتقال لـ Firebase Authentication الحقيقي

هذا **تصميم مكتوب**، مش كود جاهز للتشغيل. السبب: تسجيل الدخول شغال حاليًا
بيه كل مستخدمي النظام يوميًا — أي غلطة في التنفيذ من غير اختبار حي معاكم
خطوة بخطوة ممكن تمنع الجميع من الدخول للنظام فجأة. الخطة هنا عشان نتفق
على التصميم الأول، وبعدين ننفذه سوا تدريجيًا مع اختبار حقيقي في كل خطوة.

## ليه محتاجين الانتقال أصلًا
راجع التحذير المكتوب أعلى `firestore.rules` — باختصار: كل زائر للموقع
بياخد `isAuthenticated() = true` تلقائيًا (anonymous auth)، فنظام الأدوار
والصلاحيات بيتفحص في React بس، مش في قاعدة البيانات نفسها. يعني أي حد
يعرف رابط النظام ويفتح Developer Tools يقدر يقرا/يعدّل أي بيانات مباشرة.

## التصميم المقترح: Custom Tokens (مش Email/Password المباشر)
ليه Custom Tokens بالذات مش Firebase Auth العادي بالإيميل/الباسورد؟
لأن نظام تسجيل الدخول الحالي عنده منطق خاص (قفل الحساب بعد محاولات فاشلة،
تشفير مخصص، ترحيل تدريجي للباسوردات القديمة) بنحتاج نحافظ عليه، والـ
Custom Tokens بتخلينا نحتفظ بالتحكم الكامل في منطق التحقق من الهوية
عندنا، وبعد النجاح بس نطلب من Firebase توكن حقيقي مرتبط بالموظف.

### الخطوات
1. **Cloud Function جديدة `customLogin`** (تشبه فكرة `submitInvoiceToETA`
   الموجودة بالفعل): بتاخد email+password، تتحقق منهم بنفس منطق
   `verifyPassword` الحالي، ولو صح بتعمل
   `admin.auth().createCustomToken(employeeId, { role: employeeData.role })`.
2. **الواجهة**: بدل `verifyPassword` المباشر في `LoginScreen.jsx`، تنادي
   الـ Cloud Function دي، وتستخدم التوكن الراجع مع
   `signInWithCustomToken(auth, token)` بدل `signInAnonymously`.
3. **Custom Claims للأدوار**: نضيف الدور (role) كـ custom claim على
   التوكن، عشان `firestore.rules` تقدر تتحقق منه مباشرة بـ
   `request.auth.token.role == 'admin'` من غير ما تحتاج تقرا مستند
   الموظف في كل قاعدة (أسرع وأأمن).
4. **تحديث `firestore.rules` تدريجيًا**: كل كوليكشن على حدة، من
   `isAuthenticated()` لشرط بيتحقق من `request.auth.uid` أو
   `request.auth.token.role`، بدل تغيير الملف كامل مرة واحدة (تقليل
   مخاطر كسر شاشة معينة من غير ما نلاحظ).
5. **فترة انتقالية**: الإبقاء على `signInAnonymously` كـ fallback لأي
   مستخدم لسه ما سجلش دخول بالطريقة الجديدة، لحد ما نتأكد كل الموظفين
   عدّوا على النظام الجديد مرة واحدة على الأقل.

### الترتيب المقترح للتنفيذ (لما تكونوا جاهزين)
1. نبني الـ Cloud Function ونختبرها بمعزل عن الواجهة (Postman/curl).
2. نعدّل `LoginScreen.jsx` بس، من غير ما نلمس `firestore.rules` لسه —
   النظام هيفضل شغال بنفس مستوى الحماية الحالي، بس بتوكن حقيقي.
3. بعد ما نتأكد إن تسجيل الدخول شغال 100% لفترة (أسبوع مثلاً)، نبدأ نضيّق
   `firestore.rules` كوليكشن كوليكشن، ونختبر كل دور (admin, warehouse
   manager, call_center...) بعد كل تعديل.
4. آخر خطوة: شيل `signInAnonymously` نهائيًا.

### تقدير الوقت
- الـ Cloud Function + تعديل الواجهة: جلسة عمل واحدة مركزة.
- تضييق الـ rules تدريجيًا + الاختبار: على مدار أسبوعين تقريبًا، خطوة
  بخطوة، مش مرة واحدة.

**لما تكونوا جاهزين نبدأ، قولّي وهنبدأ بالخطوة 1 (الـ Cloud Function)
ونختبرها منعزلة الأول قبل ما نلمس شاشة تسجيل الدخول الفعلية.**
